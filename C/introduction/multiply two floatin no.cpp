//multiply two floatin point no
#include<stdio.h>
int main()
{
	double a,b,p;
	printf("Enter any two floating no for multipling them : ");
	scanf("%lf %lf",&a,&b);
    p = a*b;
	printf("Multiplication is %.5lf ",p);
	
}
